name: Publish to pub.dev

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+' # tag-pattern on pub.dev: 'v{{version}}'

jobs:
  # Run tests before publishing
  test:
    name: Test Before Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Dart
        uses: dart-lang/setup-dart@v1
        with:
          sdk: stable

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Bootstrap workspace overrides
        run: dart tool/bootstrap_workspace_pubspec_overrides.dart

      - name: Install dependencies
        run: dart pub get

      - name: Install Flutter package dependencies
        working-directory: packages/llm_dart_flutter
        run: flutter pub get

      - name: Run tests
        run: dart test

      - name: Run Flutter package tests
        working-directory: packages/llm_dart_flutter
        run: flutter test

      - name: Check workspace dependency guards
        run: dart run tool/check_workspace_dependency_guards.dart

      - name: Check root package boundary guards
        run: dart run tool/check_root_package_boundary_guards.dart

      - name: Analyze code
        run: dart analyze --fatal-infos

      - name: Analyze Flutter package code
        working-directory: packages/llm_dart_flutter
        run: flutter analyze --fatal-infos

  # Publish using the reusable workflow from dart-lang
  publish:
    name: Publish to pub.dev
    needs: test
    permissions:
      id-token: write # Required for authentication using OIDC
    uses: dart-lang/setup-dart/.github/workflows/publish.yml@v1

  # Create GitHub release
  github-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: [test, publish]
    permissions:
      contents: write        # required for creating a release
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      # Extract the release notes for the current version from CHANGELOG.md.
      - name: Extract release notes
        id: notes
        uses: ffurrer2/extract-release-notes@v2
        # No inputs needed: the action looks for the section that follows
        # "## [Unreleased]" (Keep a Changelog convention).

      # Create the GitHub release with the extracted notes.
      - name: Create GitHub release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_NOTES: ${{ steps.notes.outputs.release_notes }}
        run: |
          # Write release notes to temporary file to handle special characters safely
          printf '%s\n' "$RELEASE_NOTES" > /tmp/release_notes.txt
          gh release create "$GITHUB_REF_NAME" \
            --title "$GITHUB_REF_NAME" \
            --notes-file /tmp/release_notes.txt
