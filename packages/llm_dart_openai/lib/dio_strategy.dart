/// (Tier 3 / opt-in) OpenAI Dio strategy used by `llm_dart_provider_utils`.
///
/// This library is intentionally not part of the recommended provider
/// entrypoints. Importing it opts you into transport details that may change in
/// minor versions.
library;

export 'src/dio_strategy.dart';
