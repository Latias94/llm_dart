name: llm_dart_ollama
description: Ollama provider implementation for llm_dart, including chat, completion, embeddings and model listing for local LLMs.
version: 0.11.0-rc.1
publish_to: none

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  dio: ^5.9.0
  logging: ^1.2.0
  llm_dart_core: ^0.11.0-rc.1
  llm_dart_provider_utils: ^0.11.0-rc.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
