# Changelog

## [0.11.0-alpha.1] - 2026-04-22

- First alpha release of the focused `llm_dart_core` package.
- Publishes the historical core entrypoints as a compatibility shell over
  `llm_dart_provider` and `llm_dart_ai`.
- Keeps old foundation, model, stream, UI, and serialization import paths
  available while new code moves to the focused packages.
