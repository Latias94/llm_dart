## 0.11.0-rc.1

- Initial release as part of the `llm_dart` multi-package distribution.
- Slimmed down `LLMConfigKeys` by removing provider-specific extension keys that are now defined in provider packages (e.g. `OpenAIConfigKeys`, `GoogleConfigKeys`, `XAIConfigKeys`).
