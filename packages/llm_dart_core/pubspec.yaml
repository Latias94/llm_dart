name: llm_dart_core
description: Core interfaces and shared models for llm_dart.
version: 0.11.0-alpha.1
homepage: https://github.com/Latias94/llm_dart
repository: https://github.com/Latias94/llm_dart
issue_tracker: https://github.com/Latias94/llm_dart/issues

environment:
  sdk: '>=3.5.0 <4.0.0'

resolution: workspace

dependencies:
  # Intentionally no HTTP client dependency.
  # Providers/protocol packages use Dio (directly or via shared utilities),
  # but core must remain provider/transport-agnostic.

dev_dependencies:
  test: ^1.27.0
  lints: ^6.0.0
