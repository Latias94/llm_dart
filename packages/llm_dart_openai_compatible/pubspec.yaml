name: llm_dart_openai_compatible
description: OpenAI-compatible protocol implementation for llm_dart (shared by providers that use the OpenAI REST API shape).
version: 0.11.0-rc.1
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  dio: ^5.9.0
  llm_dart_core: ^0.11.0-rc.1
  llm_dart_provider_utils: ^0.11.0-rc.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
