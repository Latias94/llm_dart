/// (Tier 3 / opt-in) Dio transport strategy for the Anthropic-compatible client.
///
/// Re-exported from `llm_dart_anthropic_compatible` so MiniMax users don't need
/// to depend on the protocol-layer package directly.
library;

export 'package:llm_dart_anthropic_compatible/dio_strategy.dart';
