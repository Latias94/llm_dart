/// (Tier 3 / opt-in) Low-level Anthropic-compatible client (used by MiniMax).
///
/// Re-exported from `llm_dart_anthropic_compatible` so MiniMax users don't need
/// to depend on the protocol-layer package directly.
library;

export 'package:llm_dart_anthropic_compatible/client.dart';
