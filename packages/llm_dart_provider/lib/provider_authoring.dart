/// Provider-authoring contracts for custom provider implementations.
///
/// This is the explicit import target for implementations that need
/// provider-facing prompts, model request/result contracts, stream events,
/// serialization, provider specs, option resolvers, and registries.
library;

export 'foundation.dart';
