name: llm_dart_test
description: Shared testing helpers for llm_dart workspace packages.
version: 0.1.0-dev.0
publish_to: none

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  llm_dart_provider:
    path: ../llm_dart_provider
  llm_dart_transport:
    path: ../llm_dart_transport

dev_dependencies:
  lints: ^6.0.0
  test: ^1.27.0
