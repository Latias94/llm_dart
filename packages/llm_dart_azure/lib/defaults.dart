/// Defaults for the Azure OpenAI provider.
library;

/// Azure provider id used in the core registry.
const String azureProviderId = 'azure';

/// Azure OpenAI Chat Completions provider id (explicit chat surface).
const String azureChatProviderId = 'azure.chat';

/// Default Azure OpenAI API version (Vercel AI SDK parity).
const String azureDefaultApiVersion = 'v1';
