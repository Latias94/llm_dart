/// (Tier 3 / opt-in) Dio transport strategy for the OpenAI-compatible client.
///
/// Re-exported from `llm_dart_openai_compatible` so xAI users don't need
/// to depend on the protocol-layer package directly.
library;

export 'package:llm_dart_openai_compatible/dio_strategy.dart';
