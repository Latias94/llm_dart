# Goal

Build provider-owned fixture and golden contracts that make request encoding,
stream event projection, tool replay, and provider metadata behavior auditable
before further fearless refactors.

The first implementation slice should establish the convention with OpenAI
goldens while preserving the existing public API and package graph.
