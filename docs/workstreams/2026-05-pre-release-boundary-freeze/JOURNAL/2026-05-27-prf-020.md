# PRF-020 Journal — Release Ledger

Date: 2026-05-27

## Summary

Added a publish-facing release ledger under `docs/release/` plus a Dart guard.

The ledger records release posture, publishable package order, the internal
test-support package, release-facing workstreams, local gates, and non-blocking
deferrals. The guard validates package pubspec names, generated-from paths,
workstream slug/status alignment, gate tool existence, and deferral fields.

## Validation

```powershell
dart --suppress-analytics run tool/check_release_ledger.dart
```

The command passed on 2026-05-27.

## Handoff

Proceed to PRF-030 provider fixture coverage. Add fixture evidence to the
ledger only if the slice becomes a release gate.
