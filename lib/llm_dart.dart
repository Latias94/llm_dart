/// Default modern root entrypoint for `llm_dart`.
///
/// Re-exports the same stable surface as `package:llm_dart/ai.dart`.
/// Builder-era compatibility APIs now live under
/// `package:llm_dart/legacy.dart`.
library;

export 'ai.dart';
