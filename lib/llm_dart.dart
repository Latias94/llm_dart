/// Default modern root entrypoint for `llm_dart`.
///
/// Re-exports the same stable surface as `package:llm_dart/ai.dart`.
/// Builder-era root compatibility APIs have been removed; use focused provider
/// entrypoints and typed provider options for new code.
library;

export 'ai.dart';
