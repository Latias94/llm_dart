library;

/// Backwards-compatible re-export for the Ollama provider factory and
/// registration helper. The canonical implementation lives in the
/// `llm_dart_ollama` package.
export 'package:llm_dart_ollama/llm_dart_ollama.dart'
    show OllamaProviderFactory, registerOllamaProvider;
