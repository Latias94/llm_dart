library;

/// Backwards-compatible re-export for the OpenAI-compatible provider factory
/// and registrar. The canonical implementation lives in the
/// `llm_dart_openai_compatible` package.
export 'package:llm_dart_openai_compatible/llm_dart_openai_compatible.dart'
    show OpenAICompatibleProviderFactory, OpenAICompatibleProviderRegistrar;
