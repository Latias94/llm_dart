library;

/// Backwards-compatible re-export for the Groq provider factory and
/// registration helper. The canonical implementation lives in the
/// `llm_dart_groq` package.
export 'package:llm_dart_groq/llm_dart_groq.dart'
    show GroqProviderFactory, registerGroqProvider;
