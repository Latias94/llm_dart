library;

/// Backwards-compatible re-export for the DeepSeek provider factory and
/// registration helper. The canonical implementation lives in the
/// `llm_dart_deepseek` package.
export 'package:llm_dart_deepseek/llm_dart_deepseek.dart'
    show DeepSeekProviderFactory, registerDeepSeekProvider;
