library;

/// Backwards-compatible re-export for the Anthropic provider factory and
/// registration helper. The canonical implementation lives in the
/// `llm_dart_anthropic` package.
export 'package:llm_dart_anthropic/llm_dart_anthropic.dart'
    show AnthropicProviderFactory, registerAnthropicProvider;
