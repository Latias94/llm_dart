library;

/// Backwards-compatible re-export for the XAI provider factory and
/// registration helper. The canonical implementation lives in the
/// `llm_dart_xai` package.
export 'package:llm_dart_xai/llm_dart_xai.dart'
    show XAIProviderFactory, registerXAIProvider;
