import 'package:llm_dart_groq/llm_dart_groq.dart' as groq;

/// Backwards-compatible alias for the Groq provider.
///
/// The actual implementation now lives in the `llm_dart_groq` subpackage.
typedef GroqProvider = groq.GroqProvider;
